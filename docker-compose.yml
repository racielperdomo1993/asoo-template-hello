version: '3.3'
services:
  web:
    build: .
    restart: always
    ports:
      - '80'
    environment:
      DB_PASSWORD: "${password('db')}"
    volumes:
      - './html:/usr/share/nginx/html:ro'
    networks:
      - default
